# paralela-proyecto

HOLA!!!

ya saben la logica general de la app, consideraciones:

usuario es superclase de cliente (el que mira peliculas) y administrador (hay que ver que roles tiene en particular, pero el acceso por lo pronto debe ser distinto)

hable con el julian, el codigo se va a ejectuar en terminal, PERO el reproductor lo podriamos hacer con ventanas emergentes, hay que ver si usamos javafx u otro (ya tengo un prototipo en mente usando javafx, vlcj (wrapper de vlc) y vlc para reproducir (vlc lo tendria que descargar el propio programa (vlc embebido)), otra opcion a vlc es ffmpeg, pero no es reproductor completo, entonces habria que implementar todos los botones y logica (renderizado) por separado)

la parte mas complicada es la reproduccion seguido de la base de datos, la logica del resto de clases enrealidad es simple pero hay que seguir el modelado.